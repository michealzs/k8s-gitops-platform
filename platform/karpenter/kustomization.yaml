apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Only the controller chart is shared. The NodePool and EC2NodeClass differ
# per cluster, so clusters/<name>/karpenter-resources.yaml points at
# resources/<name> here.
resources:
  - application.yaml
