apiVersion: v1
kind: Namespace
metadata:
  name: podinfo
  labels:
    # Pod Security Standards: every pod in this namespace must satisfy the
    # restricted profile. The deployment below is written to pass it.
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/warn: restricted
