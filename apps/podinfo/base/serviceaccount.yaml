apiVersion: v1
kind: ServiceAccount
metadata:
  name: podinfo
  labels:
    app.kubernetes.io/name: podinfo
# podinfo never talks to the Kubernetes API, so no token is mounted.
automountServiceAccountToken: false
